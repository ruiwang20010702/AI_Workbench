# TODO：周报自动生成功能

## 待办与缺少配置
- 外部 Calendar 平台选择与 OAuth 配置（Google/Apple/Outlook）。
- Git 统计数据源选择（本地/远程）与访问令牌配置。
- 企业模板规范与样式库（Word）确认。
- 周定义与时区的用户配置项（前端设置页）。
- 导出文件的权限控制与有效期策略。

## 操作指引
- 在 `.env` 中添加第三方访问令牌与 API 地址；
- 使用模板管理页面创建企业模板，填写占位符；
- 通过周报页面选择时间范围与模板，点击一键生成；
- 若启用 AI 优化，可在预览后调用优化/建议功能并保存结果。