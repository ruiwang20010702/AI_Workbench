# ⚡ 快速执行指南 - 创建数据库函数

## 🎯 推荐方法：使用 Supabase Dashboard（最简单）

### 步骤 1: 打开 SQL 脚本文件
```bash
# 文件路径
/Users/ruiwang/Desktop/AI_Workbench/docs/数据库架构重构/create_db_functions.sql
```

### 步骤 2: 复制文件内容
- 打开上述文件
- 全选（Cmd+A）
- 复制（Cmd+C）

### 步骤 3: 在 Supabase 执行
1. 打开 [Supabase Dashboard](https://supabase.com/dashboard)
2. 选择你的项目
3. 左侧菜单 → **SQL Editor**
4. 点击 **New Query**
5. 粘贴复制的 SQL 代码
6. 点击 **Run** 按钮（或按 Cmd+Enter）
7. 等待执行完成（应该看到成功消息）

### 步骤 4: 验证创建成功
在 SQL Editor 中运行以下查询：

```sql
SELECT 
  routine_name AS function_name,
  routine_type
FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_type = 'FUNCTION'
  AND routine_name IN (
    'match_notes',
    'get_todo_stats',
    'get_project_stats',
    'get_user_activity_stats',
    'search_notes',
    'update_all_search_vectors',
    'get_recent_notes'
  )
ORDER BY routine_name;
```

**预期结果**: 应该显示 7 个函数

---

## 🧪 测试函数

创建成功后，用你的实际 `user_id` 测试：

```sql
-- 获取待办统计
SELECT * FROM get_todo_stats('12f21e7c-a87c-4c9a-a4e7-30489ace45fa');

-- 获取最近 7 天的活动
SELECT * FROM get_user_activity_stats('12f21e7c-a87c-4c9a-a4e7-30489ace45fa', 7);

-- 搜索笔记
SELECT * FROM search_notes('测试', NULL, '12f21e7c-a87c-4c9a-a4e7-30489ace45fa', 10);

-- 获取最近笔记
SELECT * FROM get_recent_notes('12f21e7c-a87c-4c9a-a4e7-30489ace45fa', 5);
```

---

## ✅ 完成！

创建成功后，这些函数将：
- ✅ 优化数据库查询性能
- ✅ 提供统计分析能力
- ✅ 支持向量相似度搜索
- ✅ 简化应用代码逻辑

---

**预计耗时**: 2-3 分钟  
**难度**: ⭐ 简单

