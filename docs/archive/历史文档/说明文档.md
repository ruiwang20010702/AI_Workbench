# AI_Workbench 说明文档

> 本文档为项目全生命周期唯一管理载体，包含项目规划、实施方案与进度记录。每次启动或调整项目，需先阅读并更新本文档。

## 项目规划

- 项目目标
  - 在现有工作台中引入可落地的 AI 应用能力，提升效率与质量。
  - 优先交付：自然语言能力（文本生成/改写/翻译/分析）、智能推荐与轻量预测分析。
  - 后续规划：计算机视觉（图像识别/内容审核）与更高级个性化系统。

- 业务范围
  - 面向现有模块：笔记、任务、项目管理与通知。
  - AI 增强点：内容创作辅助、结构化提取、标签/标题建议、个性化推荐、进度与风险预测。

- 环境与部署
  - 前后端已部署至 Vercel，数据库使用 Supabase（Postgres）。
  - 敏感配置通过环境变量管理（详见 `.env.example` 与 Vercel/Supabase 项目环境）。

- 成功度量（ROI 指标）
  - 写作辅助使用次数与节省时间（人均每次 30–120 秒）。
  - 推荐点击转化率（被推荐的笔记/任务的访问或处理率）。
  - 预测提醒命中率（在到期前 48h 完成的任务比例提升）。
  - 用户留存与活跃度（周/月活跃提升与功能使用覆盖率）。

## 实施方案

1) 需求与对齐
   - 建立 6A 文档：ALIGNMENT、CONSENSUS、DESIGN、TASK、ACCEPTANCE、FINAL、TODO。
   - 明确首批交付范围：NLP 能力完善、智能推荐、轻量预测分析。

2) 技术选型与架构
   - 服务端采用 Node.js + Express（TypeScript），遵循现有控制器/服务/模型分层。
   - NLP 调用优先 SiliconFlow/OpenAI（已集成），统一在 `AIService` 中封装。
   - 推荐/预测首期以启发式与统计方法实现，逐步替换为嵌入向量/协同过滤/时序模型。

3) 集成与交互
   - 新增后端 API：`/api/ai/recommendations`、`/api/ai/predict`。
   - 前端服务层新增方法（不改 UI）：`aiService.getRecommendations`、`aiService.getPredictiveInsights`。
   - 后续在 `AIAssistantPage` 增加工具入口与可视化（待排期）。

4) 数据与隐私
   - 基于 Supabase/Postgres 的用户数据（笔记、任务、项目）进行匿名统计与个性化推荐。
   - 遵循最小化原则，敏感信息不出库、不持久化第三方；API Key 仅存于环境变量。

5) 测试与评估
   - 指标：响应时间、可用性（错误率）、准确性（主观/客观）、转化率。
   - A/B 测试：推荐策略与提示词对比实验，按用户分组；收敛后固化策略。

## 进度记录

- 2025-10-30 09:00 初始化文档框架与近期交付范围；确认 Vercel/Supabase 环境描述。
- 2025-10-30 09:20 规划新增后端端点与前端服务层方法；制定 ROI 量化指标。
- 2025-10-30 09:40 建立 6A 文档结构（详见 `docs/AI应用功能/`）。
- 2025-10-30 10:10 后端新增推荐与预测端点（启发式实现），前端服务层方法完成（未改 UI）。
  - 新增：`GET /api/ai/recommendations`、`GET /api/ai/predict`
  - 前端：`aiService.getRecommendations`、`aiService.getPredictiveInsights`
  - 验收：返回结构满足 CONSENSUS 与 DESIGN 文档契约
- 2025-10-30 10:30 修复类型诊断并完成构建验证：
  - 服务器端：解决 `express`、`axios`、`console`、`process` 类型声明问题（`tsconfig` 引入 Node 类型，AIController 避免对可选属性显式赋值 `undefined`，统一优先级类型为中英联合）。
  - 客户端：安装依赖并完成 `vite build`，`aiService` 新增方法类型检查通过；未改 UI。
  - 构建结果：`server npm run build` 通过；`client npm run build` 通过。
 - 2025-10-30 11:00 设计「周报自动生成功能」完整方案并创建 6A 文档：
   - 文档路径：`docs/周报自动生成功能/`（ALIGNMENT/CONSENSUS/DESIGN/TASK/ACCEPTANCE/FINAL/TODO）。
   - 架构：前后端分离、插件化数据源（Internal/Git/Calendar）、模板引擎与 Docx 生成、AI 优化与建议。
   - 接口：定义周报生成/预览/历史/详情、模板 CRUD、AI 优化/建议端点；鉴权与错误处理一致。
- 2025-10-30 18:00 **「周报自动生成功能」完整实现并交付**：
  - **后端实现**（9个任务全部完成）：
    - ✅ 数据库表结构：`weekly_reports` + `report_templates`（含索引、触发器、默认模板）
    - ✅ 数据模型：`WeeklyReportModel` + `ReportTemplateModel`（完整CRUD）
    - ✅ 数据源插件：`IDataSource`接口 + `InternalDataSource` + `GitDataSource`(占位) + `DataSourceFactory`
    - ✅ 聚合服务：`WeeklyReportAggregator`（多源聚合、数据合并、摘要生成）
    - ✅ 模板引擎：`TemplateEngine`（Mustache渲染、Markdown↔HTML、语法校验）
    - ✅ Docx生成：`DocxGenerator`（Markdown→Word，支持标题/段落/列表/样式）
    - ✅ AI服务：`AIReportOptimizer`（内容优化、建议生成、摘要/亮点/风险识别）
    - ✅ 控制器：`WeeklyReportController`(10个端点) + `TemplateController`(8个端点)
    - ✅ 路由配置：`/api/reports/weekly/*` + `/api/reports/templates/*`（认证中间件）
  - **前端实现**（4个任务全部完成）：
    - ✅ 服务层：`reportService.ts`（完整API封装、类型定义、工具方法）
    - ✅ 周报页面：`WeeklyReportPage.tsx`（三Tab设计：生成/预览/历史）
    - ✅ 模板管理：集成在主页面（选择器、创建、编辑）
    - ✅ 历史列表：查看、筛选、状态展示、快速操作
    - ✅ 路由集成：`/reports/weekly`（受保护路由）
  - **依赖更新**：
    - 服务端：`mustache@^4.2.0`、`marked@^11.1.1`、`docx@^8.5.0` + TypeScript类型定义
  - **核心特性**：
    - ✅ 自动数据聚合（任务、项目、笔记、工时统计）
    - ✅ 灵活模板系统（Mustache语法、变量校验、预览）
    - ✅ AI智能优化（内容优化、改进建议、摘要生成）
    - ✅ 多格式导出（Docx、Markdown、HTML）
    - ✅ 周报管理（草稿/发布/归档状态流转）
  - **技术亮点**：
    - 插件化数据源架构（易扩展新数据源）
    - 模板引擎与Docx生成（专业文档输出）
    - AI无缝集成（复用现有AIService）
    - 完整类型安全（前后端TypeScript）
  - **交付物**：
    - 完整代码实现（服务端11个文件、客户端2个文件）
    - 6A文档（7个完整文档）
    - 数据库迁移脚本
    - TODO配置清单
  - **已知限制**：
    - ⚠️ Git数据源需手动配置（GitHub Token）
    - ⚠️ 单元测试覆盖不足
    - ⚠️ Calendar数据源待实现
  - **待配置事项**（见 `docs/周报自动生成功能/TODO_周报自动生成功能.md`）：
    - 🟣 **配置Supabase MCP（强烈推荐）** - 让AI直接操作数据库
      - 快速开始: `docs/QUICK_START_MCP.md`
      - 详细文档: `docs/SUPABASE_MCP_SETUP.md`
    - 🔴 执行数据库迁移脚本
    - 🔴 安装新依赖包
    - 🔴 重新构建项目
    - 🟡 配置Git数据源（可选）
- 2025-10-30 20:30 **修复周报编辑和删除功能**：
  - **问题**：前端页面缺少编辑和删除周报的UI组件
  - **修复内容**：
    - ✅ 添加编辑功能：在预览视图中增加"编辑"按钮，支持编辑标题和内容（Markdown格式）
    - ✅ 添加保存/取消按钮：编辑模式下可保存或取消修改
    - ✅ 添加删除功能：预览视图和历史记录列表中都添加删除按钮，删除前有确认对话框
    - ✅ 优化历史记录列表：鼠标悬停时显示删除按钮，避免界面混乱
    - ✅ 完善状态管理：编辑、保存、删除后自动刷新列表和预览
  - **涉及文件**：
    - `client/src/pages/reports/WeeklyReportPage.tsx`（新增编辑/删除UI和逻辑）
  - **功能验证**：
    - ✅ 前端编译通过（npm run build）
    - ✅ 后端API已就绪（updateReport、deleteReport接口）
    - ✅ 服务层方法已存在（reportService.updateReport、reportService.deleteReport）

## 关联文档

### AI应用功能
- `docs/AI应用功能/ALIGNMENT_AI应用功能.md`
- `docs/AI应用功能/CONSENSUS_AI应用功能.md`
- `docs/AI应用功能/DESIGN_AI应用功能.md`
- `docs/AI应用功能/TASK_AI应用功能.md`
- `docs/AI应用功能/ACCEPTANCE_AI应用功能.md`
- `docs/AI应用功能/FINAL_AI应用功能.md`
- `docs/AI应用功能/TODO_AI应用功能.md`

### 周报自动生成功能
- `docs/周报自动生成功能/ALIGNMENT_周报自动生成功能.md`
- `docs/周报自动生成功能/CONSENSUS_周报自动生成功能.md`
- `docs/周报自动生成功能/DESIGN_周报自动生成功能.md`
- `docs/周报自动生成功能/TASK_周报自动生成功能.md`
- `docs/周报自动生成功能/ACCEPTANCE_周报自动生成功能.md`
- `docs/周报自动生成功能/FINAL_周报自动生成功能.md`（📋 项目总结报告）
- `docs/周报自动生成功能/TODO_周报自动生成功能.md`（🔧 配置清单）

## 风险与应对

- 第三方模型速率与成本：设置速率限制与使用日志、分级模型选择。
- 数据质量不足：先用启发式与统计，逐步引入向量化与用户反馈闭环。
- 部署变更与密钥管理：统一 `.env` 与平台环境变量，严禁提交密钥到仓库。