# AI Workbench 快速启动指南

**更新时间**: 2025-11-01  
**状态**: ✅ 环境已配置完成

---

## 🎯 当前状态

- ✅ Supabase 数据库已连接
- ✅ 环境变量已配置
- ✅ 所有必需的数据库表已创建
- ✅ `pending_members` 表已就绪
- 🚀 可以启动应用进行测试

---

## 🚀 启动步骤

### 1. 启动后端服务器

```bash
cd /Users/ruiwang/Desktop/AI_Workbench/server
npm install  # 首次运行需要安装依赖
npm run dev
```

**预期输出**:
```
🚀 Server is running on http://localhost:5000
✅ Connected to Supabase
```

**访问**: http://localhost:5000/api/health

---

### 2. 启动前端应用

打开新的终端窗口：

```bash
cd /Users/ruiwang/Desktop/AI_Workbench/client
npm install  # 首次运行需要安装依赖
npm run dev
```

**预期输出**:
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**访问**: http://localhost:5173

---

## 🧪 测试待定成员功能

### 测试流程

1. **注册/登录用户**
   - 访问 http://localhost:5173
   - 注册新用户或使用现有账户登录

2. **创建项目**
   - 进入项目管理页面
   - 创建一个新项目

3. **添加待定成员**
   - 进入项目详情页
   - 点击"添加成员"
   - 输入邮箱地址（不存在的用户）
   - 选择角色（member/admin）
   - 点击"发送邀请"

4. **验证待定成员**
   - 查看项目成员列表
   - 应该看到待定成员显示为"待定"状态
   - 可以取消邀请

5. **接受邀请（模拟）**
   - 使用邀请邮箱注册新用户
   - 系统应自动将待定成员转为正式成员

---

## 🔧 环境配置详情

### Server 环境变量 (`server/.env`)

```env
# 服务器配置
NODE_ENV=development
PORT=5000

# Supabase 配置
SUPABASE_URL=https://dkczfihkowivzcvsnxpo.supabase.co
SUPABASE_ANON_KEY=已配置 ✅
SUPABASE_SERVICE_ROLE_KEY=已配置 ✅
SUPABASE_PROJECT_ID=dkczfihkowivzcvsnxpo
SUPABASE_ACCESS_TOKEN=已配置 ✅

# JWT 配置
JWT_SECRET=your-super-secret-jwt-key-change-in-production-at-least-64-chars
JWT_EXPIRES_IN=7d

# AI 服务（可选）
SILICONFLOW_API_KEY=需要配置（如使用 AI 功能）
SILICONFLOW_API_BASE_URL=https://api.siliconflow.cn/v1

# CORS 配置
CORS_ORIGIN=http://localhost:5173,http://localhost:3000
```

### Client 环境变量 (`client/.env`)

```env
# API 地址
VITE_API_URL=http://localhost:5000/api

# Supabase 配置
VITE_SUPABASE_URL=https://dkczfihkowivzcvsnxpo.supabase.co
VITE_SUPABASE_ANON_KEY=已配置 ✅
```

---

## 📊 数据库表状态

所有必需的表都已创建：

| 表名 | 状态 | 说明 |
|------|------|------|
| `users` | ✅ | 用户表 |
| `projects` | ✅ | 项目表 |
| `tasks` | ✅ | 任务表 |
| `todos` | ✅ | 待办事项表 |
| `notes` | ✅ | 笔记表 |
| `tags` | ✅ | 标签表 |
| `note_tags` | ✅ | 笔记标签关联表 |
| `ai_usage_logs` | ✅ | AI 使用日志表 |
| `pending_members` | ✅ | 待定成员表 ⭐ |

---

## 🔍 验证连接

### 测试 Supabase 连接

```bash
cd /Users/ruiwang/Desktop/AI_Workbench/server
node -e "
const { createClient } = require('@supabase/supabase-js');
require('dotenv').config();
const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY);
supabase.from('pending_members').select('*', { count: 'exact', head: true })
  .then(({ error }) => {
    if (error) console.log('❌', error.message);
    else console.log('✅ pending_members 表连接成功！');
  });
"
```

### 测试 API 健康检查

```bash
curl http://localhost:5000/api/health
```

**预期响应**:
```json
{
  "status": "ok",
  "timestamp": "2025-11-01T10:35:00.000Z"
}
```

---

## 📚 相关文档

- [环境配置完成报告](./docs/待定成员功能/环境配置完成.md)
- [待定成员功能 TODO](./docs/待定成员功能/TODO_待定成员功能.md)
- [待定成员功能验收报告](./docs/待定成员功能/ACCEPTANCE_待定成员功能.md)
- [Supabase Dashboard](https://app.supabase.com/project/dkczfihkowivzcvsnxpo)

---

## ⚠️ 可选配置

### 1. 更新 JWT Secret（生产环境必须）

```bash
# 生成强随机密钥
node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# 将生成的密钥更新到 server/.env
JWT_SECRET=<生成的密钥>
```

### 2. 配置 AI 服务（如需使用 AI 功能）

```bash
# 编辑 server/.env
SILICONFLOW_API_KEY=your-api-key-here
```

获取 API Key: https://siliconflow.cn

### 3. 配置 Git 数据源（如需周报功能）

```bash
# 编辑 server/.env
GIT_DATASOURCE_ENABLED=true
GIT_PROVIDER=github
GITHUB_TOKEN=your-github-token
GITHUB_USERNAME=your-username
GITHUB_REPOSITORIES=repo1,repo2
```

---

## 🐛 故障排除

### 问题 1: 端口已被占用

**错误**: `Error: listen EADDRINUSE: address already in use :::5000`

**解决方案**:
```bash
# 查找占用端口的进程
lsof -i :5000

# 终止进程
kill -9 <PID>

# 或更改端口（编辑 server/.env）
PORT=5001
```

### 问题 2: 模块未找到

**错误**: `Cannot find module 'xxx'`

**解决方案**:
```bash
# 重新安装依赖
cd server  # 或 cd client
rm -rf node_modules package-lock.json
npm install
```

### 问题 3: 数据库连接失败

**错误**: `connection refused` 或 `ECONNREFUSED`

**解决方案**:
1. 检查 `.env` 文件是否存在
2. 验证 `SUPABASE_URL` 和 `SUPABASE_SERVICE_ROLE_KEY` 是否正确
3. 检查网络连接
4. 访问 Supabase Dashboard 确认项目状态

---

## 🎉 开始使用

一切准备就绪！现在您可以：

1. ✅ 启动应用
2. ✅ 测试待定成员功能
3. ✅ 开发新功能
4. ✅ 运行测试

**祝您使用愉快！** 🚀

---

**最后更新**: 2025-11-01 18:35  
**配置状态**: ✅ 完成  
**可用功能**: 全部核心功能 + 待定成员功能

